import { ProjectOwnerPipe } from './project-owner.pipe';

describe('ProjectOwnerPipe', () => {
  it('create an instance', () => {
    const pipe = new ProjectOwnerPipe();
    expect(pipe).toBeTruthy();
  });
});
