import { RiskStatusPipe } from './risk-status.pipe';

describe('RiskStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new RiskStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
