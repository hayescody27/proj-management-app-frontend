import { PasswordMatchErrorStateMatcher } from './password-match-error-state-matcher';

describe('PasswordMatchErrorStateMatcher', () => {
  it('should create an instance', () => {
    expect(new PasswordMatchErrorStateMatcher()).toBeTruthy();
  });
});
